## 2025-04-18 - Missing ARIA Labels on Icon Buttons
**Learning:** Found multiple icon-only buttons lacking `aria-label` or `title` across various pages, which makes them inaccessible to screen readers.
**Action:** Always verify icon-only buttons include descriptive `aria-label` attributes.
